// Parameter wird erst in der index.js gefüllt mit den Ararys aus der data

export const sorting = (array) => {

    return array.sort()
}

// console.log(sorting(arrayCars))
// console.log(sorting(arrayNumbers))

